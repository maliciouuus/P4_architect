[pytest]
# Configuration des tests E2E Playwright
# L'application doit tourner sur http://localhost:5173 avant de lancer les tests

addopts = --screenshot=only-on-failure
base_url = http://localhost:5173
